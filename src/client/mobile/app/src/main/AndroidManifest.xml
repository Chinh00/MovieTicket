<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MovieTicketApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieTicket"
        tools:targetApi="31">
        <!--<activity android:name=".ui.auth.LoginActivity" android:exported="true">
        </activity>-->
        <activity android:name=".ui.systems.ErrorActivity" android:exported="true" />
        <activity android:name=".ui.detail.view.DetailActivity"/>
        <activity android:name=".ui.order.screening.ScreenActivity"/>
        <activity android:name=".ui.order.ticket.TicketBookActivity"/>
        <activity android:name=".ui.order.food.OrderFoodActivity"/>
        <activity android:name=".ui.main.MainActivity"/>


        <activity android:name=".ui.auth.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>